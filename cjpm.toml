[package]
cjc-version = "1.0.0"
name = "pipeline"
description = "Sample Cangjie project used to verify the CI unit-test pipeline."
version = "0.1.0"
output-type = "executable"
src-dir = "src/pipeline"
target-dir = "target"
